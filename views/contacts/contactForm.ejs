<!DOCTYPE html>
<html>
  <head>
    <title>NodeJS Course 2024</title>
    <link
      rel="shortcut icon"
      href="https://i.ibb.co/PTYrggs/favicon-32x32.png" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&display=swap"
      rel="stylesheet" />
  </head>
  <body>
    <div class="wrapper">
      <main class="page">
        <section class="home">
          <%if(contact.id){%>
          <h1 class="home__title">Редагувати контакт</h1>
          <%}else{%>
          <h1 class="home__title">Додати новий контакт</h1>
          <%}%>
          <form
            action="<%= contact.id ? '/contacts/edit/' + contact.id : '/contacts/create' %>"
            method="post"
            class="form">
            <label for="userName">
              Iм'я
              <input
                required
                type="text"
                name="userName"
                value="<%=contact.userName? contact.userName : ''%>" />
            </label>
            <label for="userNumber">
              Номер
              <input
                required
                type="number"
                name="userNumber"
                value="<%=contact.userNumber? contact.userNumber : ''%>" />
            </label>
            <label for="userLocation">
              Місто
              <input
                required
                type="text"
                name="userLocation"
                value="<%=contact.userLocation? contact.userLocation : ''%>" />
            </label>
            <button type="submit" class="button mini">Зберегти контакт</button>
          </form>
          <a href="/contacts" class="button"> Назад до контактів </a>
        </section>
      </main>
      <footer class="footer">Created with love by Ruslan Musienko</footer>
    </div>
  </body>
</html>
